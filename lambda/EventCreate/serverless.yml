service: EventCreate

provider:
  name: aws
  runtime: nodejs12.x

  stage: dev
  region: us-east-1

functions:
  hello:
    handler: index.handler
    timeout: 1
    environment:
      PGDATABASE: db1
      PGPORT: 5432
      PGHOST: database-1.cbnw6nneqhcp.us-east-1.rds.amazonaws.com
